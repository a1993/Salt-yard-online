#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 运行代码检查..."

# 运行 linter
npm run lint

# 检查退出码
if [ $? -ne 0 ]; then
  echo "❌ 代码检查失败，请修复问题后再提交"
  exit 1
fi

echo "✅ 代码检查通过"


