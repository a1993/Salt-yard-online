# Tailwind CSS 环境差异说明

## 🤔 为什么不同环境会有不同差异？

### 1. **Tailwind CSS 的工作原理**

Tailwind CSS 使用 **JIT (Just-In-Time)** 模式：

- ✅ **只生成实际使用的类**
- ✅ 扫描代码中使用的 Tailwind 类
- ✅ 只将这些类添加到 CSS 文件中

### 2. **不同环境可能使用不同的类**

#### 场景 1：不同分支使用不同的类

```bash
# 分支 A (main)
class="bg-blue-500 text-white"

# 分支 B (feature/new-design)
class="bg-red-500 text-black"
```

**结果：**

- 分支 A 的 `tailwind.output.css` 包含 `bg-blue-500`
- 分支 B 的 `tailwind.output.css` 包含 `bg-red-500`
- **文件内容不同！**

#### 场景 2：开发环境 vs 生产环境

```bash
# 本地开发：使用了很多测试类
class="bg-blue-500 bg-red-500 bg-green-500 text-3xl text-4xl"

# 生产环境：只使用必要的类
class="bg-blue-500 text-3xl"
```

**结果：**

- 本地构建的 CSS 文件更大（包含更多类）
- 生产构建的 CSS 文件更小（只包含使用的类）

### 3. **为什么不能提交 `tailwind.output.css`？**

| 问题         | 说明                                          |
| ------------ | --------------------------------------------- |
| **合并冲突** | 不同分支生成的 CSS 不同，合并时容易冲突       |
| **文件大小** | 可能包含未使用的类，文件变大                  |
| **不一致**   | 不同开发者本地构建可能略有不同                |
| **可重现性** | 任何人都可以通过 `npm run build:css` 重新生成 |

## 🎯 解决方案：使用 `content` 配置

Tailwind CSS 通过 `tailwind.config.js` 中的 `content` 配置来扫描文件：

```javascript
module.exports = {
  content: [
    './layout/**/*.liquid',
    './sections/**/*.liquid',
    './snippets/**/*.liquid',
    './templates/**/*.liquid',
    './templates/**/*.json',
    './assets/**/*.js',
  ],
};
```

### 工作原理

1. **扫描阶段**：Tailwind 扫描 `content` 中指定的所有文件
2. **提取类名**：找出所有使用的 Tailwind 类（如 `bg-blue-500`）
3. **生成 CSS**：只为这些类生成样式

### 示例

假设你的代码中有：

```liquid
<!-- layout/theme.liquid -->
<main class='bg-blue-500 text-3xl'>
  <div class='container'>
    <h1 class='text-2xl font-bold'>标题</h1>
  </div>
</main>
```

**Tailwind 会生成：**

- ✅ `.bg-blue-500` - 因为使用了
- ✅ `.text-3xl` - 因为使用了
- ✅ `.container` - 因为使用了
- ✅ `.text-2xl` - 因为使用了
- ✅ `.font-bold` - 因为使用了
- ❌ `.bg-red-500` - **不会生成**（因为没使用）

## 📊 不同分支的差异示例

### 分支 A: `main`

```liquid
<!-- 使用的类 -->
class="bg-blue-500 text-white p-4"
```

**生成的 CSS：**

```css
.bg-blue-500 { ... }
.text-white { ... }
.p-4 { ... }
```

### 分支 B: `feature/new-design`

```liquid
<!-- 使用的类 -->
class="bg-red-500 text-black p-8"
```

**生成的 CSS：**

```css
.bg-red-500 { ... }
.text-black { ... }
.p-8 { ... }
```

**差异：**

- 分支 A 有 `bg-blue-500`，分支 B 没有
- 分支 B 有 `bg-red-500`，分支 A 没有
- **这就是为什么不能提交构建产物！**

## 🔄 工作流程

### 1. 开发阶段

```bash
# 本地开发
npm run watch:css  # 自动检测并生成使用的类
```

### 2. 提交代码

```bash
# 只提交源代码，不提交 tailwind.output.css
git add layout/theme.liquid
git commit -m "添加蓝色背景"
git push
```

### 3. CI/CD 阶段

```bash
# GitHub Actions 自动执行
npm run build:css  # 重新扫描所有文件，生成最新的 CSS
shopify theme push  # 部署到 Shopify
```

## ✅ 最佳实践

### 1. **始终在 CI/CD 中构建**

```yaml
# .github/workflows/deploy.yml
- name: Build Tailwind CSS
  run: npm run build:css
```

**原因：**

- ✅ 确保使用最新的代码扫描
- ✅ 包含所有分支使用的类
- ✅ 避免本地环境差异

### 2. **使用 `content` 配置扫描所有文件**

```javascript
// tailwind.config.js
content: [
  './layout/**/*.liquid',
  './sections/**/*.liquid',
  // ... 所有可能使用 Tailwind 的文件
];
```

### 3. **不要提交 `tailwind.output.css`**

```gitignore
# .gitignore
assets/tailwind.output.css
```

### 4. **本地开发使用 watch 模式**

```bash
npm run watch:css  # 自动检测变化并重新构建
```

## 🎯 总结

| 问题                       | 答案                               |
| -------------------------- | ---------------------------------- |
| **为什么不同环境有差异？** | 因为不同环境使用的 Tailwind 类不同 |
| **为什么不能提交 CSS？**   | 避免合并冲突，保持文件一致性       |
| **如何确保一致性？**       | 在 CI/CD 中统一构建                |
| **不同分支怎么办？**       | 每个分支构建时只包含该分支使用的类 |

## 💡 关键点

1. ✅ **Tailwind 只生成使用的类** - 这是 JIT 模式的核心
2. ✅ **不同分支使用不同类** - 所以构建结果不同
3. ✅ **在 CI/CD 中构建** - 确保使用最新代码
4. ✅ **不要提交构建产物** - 避免冲突和不一致
